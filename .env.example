# Pack Service Configuration
# Copy this file to .env and update values for your environment
# Generate secure keys with: go run scripts/generate_keys.go

PORT=8080
APP_VERSION=1.0.0

# MongoDB Configuration
MONGODB_ENABLED=true
MONGODB_URI=mongodb://pack_user:pack_password@localhost:27017/pack_service?authSource=pack_service
MONGODB_DATABASE=pack_service

# MongoDB Docker (used by docker-compose)
MONGO_ROOT_USER=root
MONGO_ROOT_PASSWORD=rootpassword
MONGO_APP_USER=pack_user
MONGO_APP_PASSWORD=pack_password
MONGO_PORT=27017

# Rate Limiting
RATE_LIMIT=100
RATE_WINDOW=1m

# Cache Configuration
CACHE_SIZE=1000
CACHE_TTL=5m

# Pack Sizes (comma-separated)
PACK_SIZES=250,500,1000,2000,5000

# ============================================
# Authentication Configuration
# ============================================
AUTH_ENABLED=false

# API Keys (comma-separated, for simple API key auth)
# Generate with: go run scripts/generate_keys.go
API_KEYS=

# JWT Configuration (required if AUTH_ENABLED=true)
# IMPORTANT: Generate secure keys for production!
# Generate with: go run scripts/generate_keys.go
JWT_SECRET_KEY=CHANGE_ME_generate_with_scripts_generate_keys_go
JWT_REFRESH_SECRET_KEY=CHANGE_ME_generate_with_scripts_generate_keys_go
JWT_ACCESS_TOKEN_TTL=15m
JWT_REFRESH_TOKEN_TTL=168h

# ============================================
# CORS Configuration
# ============================================
# Comma-separated list of allowed origins (localhost always included)
CORS_ORIGINS=

# ============================================
# Swagger UI Protection (optional)
# ============================================
# Leave empty for open access (local development)
SWAGGER_USER=
SWAGGER_PASS=

# ============================================
# Circuit Breaker Configuration
# ============================================
CIRCUIT_BREAKER_FAILURE_THRESHOLD=5
CIRCUIT_BREAKER_SUCCESS_THRESHOLD=2
CIRCUIT_BREAKER_TIMEOUT=30s

# MongoDB Logs TTL (how long to keep request logs)
MONGODB_LOGS_TTL=720h
